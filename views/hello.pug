extends layout

block content
  div.wrapper.container
    - const upName = name.toUpperCase();
    p.hello Hello #{upName}
    span#yo Yo!!
    img.dog(src="dog.jpg" alt=`${age}`)

  h2
    | Hello 
    em  How are you?
  h3 Sale ends #{h.moment().endOf('day').fromNow()}